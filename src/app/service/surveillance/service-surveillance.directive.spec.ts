import { ServiceSurveillance } from './service-surveillance';

describe('ServiceSurveillanceDirective', () => {
  it('should create an instance', () => {
    const directive = new ServiceSurveillance();
    expect(directive).toBeTruthy();
  });
});
